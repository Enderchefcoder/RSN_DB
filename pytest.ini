[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    integration: tests that need MemPalace/ChromaDB installed

[tool:pytest.ini_options]
filterwarnings =
    ignore::DeprecationWarning
